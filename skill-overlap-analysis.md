# Skill overlap analysis

I read the skill metadata under `C:\Users\bafna_sb19qr0\.agents\skills`. The biggest overlaps are:

## 1) UI / design / front-end design
- `impeccable`
- `design-taste-frontend`
- `hallmark`
- `landing-page-design`
- `extract-design`
- These all cover redesigns, UX critique, landing pages, design systems, and visual polish. `impeccable` is the broadest; `landing-page-design` and `hallmark` are narrower sub-areas.

## 2) Code quality / architecture / review
- `code-review`
- `code-simplification`
- `dsa-codebase-audit`
- `improve-codebase-architecture`
- `diagnosing-bugs`
- These overlap on code audits, refactoring, architecture issues, and technical quality review. `code-review` is PR/diff-focused; `dsa-codebase-audit` and `improve-codebase-architecture` are broader system-level.

## 3) Decision-making / problem solving
- `decide`
- `idea-refine`
- `grilling`
- `unstuck`
- `business-brainstorm`
- `maker-council`
- These are all about narrowing choices, figuring out what to build, handling blockers, and making calls. `unstuck` and `decide` are especially adjacent; `business-brainstorm` is a domain-specific version.

## 4) Web content / browser / extraction
- `agent-browser`
- `firecrawl`
- `social-fetch`
- `watch-video`
- These all pull data from the web or browser-based interfaces. `agent-browser` overlaps most with web automation and scraping; `social-fetch`/`watch-video` are narrower content-extraction variants.

## 5) Conversion from discussion to concrete artifacts
- `to-spec`
- `to-tickets`
- `implement`
- `teach`
- These all turn loose conversation or intent into structured work, but `to-spec`/`to-tickets` are more planning-focused and `implement` is execution-focused.

## 6) Skill-repo / workflow meta
- `skill-creator`
- `skill-judge`
- `audit-config`
- `find-skills`
- `setup-matt-pocock-skills`
- `writing-for-agents`
- This cluster is about improving / maintaining the skill system itself rather than product work.

## Most notable near-duplicates
- `impeccable` vs `design-taste-frontend` vs `hallmark`
- `code-review` vs `dsa-codebase-audit` vs `code-simplification`
- `decide` vs `idea-refine` vs `unstuck`
- `agent-browser` vs `firecrawl`
- `to-spec` vs `to-tickets`

If you want, I can turn this into a cleaner “recommended unique roles” list and suggest which ones should be merged or kept separate.
